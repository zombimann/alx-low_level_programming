#include <stdio.h>
#include "main.h"

/**
 * _puts - puts a string to stdout, followed by a newline
 * @str - The string to be printed
 *
 * Return: Nothing
 */

void _puts(char *str)
{
	printf("%s", str);
}
