#include <stdio.h>

/**
 * main - Entry point
 *
 * Returns: zero for successful executtion
 */

int main (void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
